# Enable AndroidX artifacts
android.useAndroidX=true
# Enable Jetifier (transforms old support libs to AndroidX) — safe to enable
android.enableJetifier=true

# Suppress the plugin/compileSdk mismatch warning (optional)
android.suppressUnsupportedCompileSdk=34
